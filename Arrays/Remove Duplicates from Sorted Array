class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int low = 0;
        int n=nums.size();
        for(int i=0; i<n; i++){
            if(nums[low]!=nums[i]){
                nums[++low] = nums[i];
            }
        }
        return low+1;
    }
};
